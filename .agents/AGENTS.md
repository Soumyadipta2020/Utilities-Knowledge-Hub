# Agent Rules

- **Full Autonomy & Direct Execution**: Always assume the user has no direct access or tools to execute manual commands, run scripts, test APIs, or edit files. The AI should autonomously perform all required tasks, command executions, setup, testing, and file operations end-to-end using its available tools.
